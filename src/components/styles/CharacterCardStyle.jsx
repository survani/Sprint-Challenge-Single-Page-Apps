import styled from 'styled-components';

//Styles for the welcome page...
const CharacterCardStyle = styled.section`
     display: flex;
     flex-wrap: wrap;
     flex-direction: column;
     align-items: center;
`;

export default CharacterCardStyle;